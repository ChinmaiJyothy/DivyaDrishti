# Services

API service functions and external integrations. One file per domain.

Examples:
- `auth.service.ts`
- `profile.service.ts`
- `conversation.service.ts`

Do not connect to backend APIs until the frontend foundation is approved.
