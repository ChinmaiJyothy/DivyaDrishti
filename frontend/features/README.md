# Features

Each feature is a self-contained domain module containing its own components, hooks, services, and validation schemas.

Examples:
- `auth/` — authentication pages and forms
- `chat/` — conversation and chat UI
- `charts/` — birth chart rendering and controls
- `admin/` — administration interfaces

Do not implement business logic until the frontend foundation is approved.
