# Styles

Global styles, theme variables, and Tailwind customization.

- `globals.css` lives in `app/globals.css` to keep Next.js conventions.
- Tailwind configuration is in `tailwind.config.ts`.
- Add utility classes or component overrides here when needed.
