# Repositories

This package contains repository implementations for data access.

## Purpose

- Abstract database queries
- Provide CRUD operations for domain entities
- Enable swapping persistence implementations
